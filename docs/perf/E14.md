# E14 冗余代码简化与性能 — 基线存档

Issue: https://github.com/ceer-quant/BlitzkriegBot/issues/96（P0，v0.2 RC-Hardening）

## 验收标准（issue 原文映射）

| 验收标准 | 基线证据 | 目标 |
| --- | --- | --- |
| 归档 udeps 输出 | 本文件 §2（工具不可用，等效证据链） | 清理未用依赖 |
| 先测 LOC 基线再达成目标 | `e14-loc.json`：**45,626 行** | −10%（≈ 41,063 行） |
| 归档 cargo bench 结果 | `e14-bench-baseline.txt`（criterion `e14-baseline` 基线已保存） | 热路径 P99 −20% |
| 内存从基线到目标 | `e14-mem-baseline.json`：**峰值 17,472 KB / 稳态 14,352 KB** | −15%（峰值 ≈ 14,851 KB） |
| 确认无阻塞主线程的调用 | 本文件 §5（审计清单，修复后复核） | Tokio 主线程零阻塞 |

## 1. LOC 基线

`node scripts/e14-loc-count.mjs --json docs/perf/e14-loc.json`（只统计主 workspace 成员 `src/**/*.rs`）：

| crate | 行数 |
| --- | --- |
| blitzkrieg-core | 29,233 |
| blitzkrieg-ui-kit | 6,168 |
| polymarket-extension | 1,944 |
| blitzkrieg-ui-panel | 3,147 |
| strategy-logic | 2,731 |
| blitzkrieg-strategy-api | 1,349 |
| blitzkrieg-market-api | 666 |
| parity-logic | 388 |
| **合计** | **45,626** |

## 2. 未用依赖清理

**工具可用性说明（诚实归档）**：`cargo +nightly udeps` 需要 nightly 工具链。本机为 Homebrew rust 1.97.1（无 rustup，装 nightly 会影响用户环境），因此 udeps 输出无法产生。等效证据链为：

1. `cargo machete`（静态 Cargo.toml 级检测）指认候选；
2. 逐项 `grep -rn "依赖名"` 核对源码零引用；
3. 删除后 `cargo check --all-targets --workspace` 全目标编译通过（引用会被编译器拒绝）；
4. 复跑 machete 清洁。

已删除 9 项：

- `blitzkrieg_core`：tokio-util、tokio-tungstenite、futures、futures-util、thiserror
- `blitzkrieg-market-api`：tokio（sync feature）
- `polymarket-extension`：async-trait、serde（直接依赖；走 workspace 透传）、thiserror；futures → 仅保留 `futures-util`（`venue.rs` 的 `StreamExt` 与 `feed.rs` 的 WS 在用），tokio-tungstenite 保留（binance WS feed）

`Cargo.lock` 145,881 → 145,725 字节。

## 3. criterion 微基准基线

`core/blitzkrieg_core/benches/hot_path.rs`（新增，`--save-baseline e14-baseline`）。fixture：固定时钟（round horizon = 基准时钟 + 100h，保证测量期间轮次不切换、不过 min_time_left 门禁）、4 tokens × 20 档盘口、2-tick 价差、Taker 按对手价开仓并断言仓位保持。

| bench | 中位时间 | P99 参考（上界） |
| --- | --- | --- |
| book_apply/depth/5 | 5.588 µs | 5.768 µs |
| book_apply/depth/20 | 8.883 µs | 9.070 µs |
| evaluate/tokens/1 | 218.8 ns | 222.9 ns |
| evaluate/tokens/4 | 788.4 ns | 798.2 ns |
| engine_on_data/4tok | 9.877 µs | 10.525 µs |
| tick/1pos | 1.142 µs | 1.162 µs |

复现：`cargo bench -p blitzkrieg-core -- --save-baseline e14-baseline`

## 4. 内存基线

`node scripts/e14-memory-baseline.mjs --seconds 30`：dry 模式 core，40 市场（`engine.markets`）+ 40 tokens × 20 档全书按 ~20Hz 刷新（`engine.book`），IPC 维护循环节奏同生产（50ms），每 250ms 从 `ps` 采样 RSS：

- **峰值 17,472 KB**（第 2 秒，启动加载期）
- **稳态 14,352 KB**（之后全程平稳无爬升——无泄漏迹象）

## 5. Tokio/async 阻塞审计（已修复）

主 workspace 内 `block_on`/`tokio::time::sleep` 直用为零；嫌疑点均为「std 同步原语/同步等待在 async 上下文」，修复方式：同步等待包 `tokio::task::spawn_blocking`，需要结果的用闭包返回 tuple 拿回所有者（显式 stop 置 `owns=false`，Drop 变 no-op）：

1. ✅ `ui/ui_kit_panel/src/bin/blitzkrieg.rs:586` — `run_unified` 的 `supervisor.start()`（内含最长 15s STARTUP_DEADLINE 同步轮询）→ spawn_blocking；
2. ✅ `run_core_subcommand` 的 start/stop 同模式 → spawn_blocking；
3. ✅ 信号分支 `d.stop()`（≤5s TERM_GRACE）+ `Command::status` → spawn_blocking（`run_unified` 信号任务、web 尾部、TUI 后、main 的 stop 分支、lifecycle start 共 7 处）；
4. ✅ `Supervisor::Drop` 链路 — 所有 spawn_blocking 闭包在显式 stop 后返回 `(sup, …)` 并置 `owns=false`，控制流结束的隐式 Drop 为 no-op；panic/中断路径的 Drop 仍兜底 stop（在阻塞线程里执行，不占 async worker）。

另有 `ui/ui_kit_panel/src/lib.rs` 的 refresher：事件触发 refresh 与轮询 loop 各自持有完整闭包副本（双份逐字重复），已合并为 `fetch_snapshot_once` helper（返回 `Option<(UiSnapshot, managed, pid)>`），两处调用均包 spawn_blocking。

## 6. 重复逻辑（已执行 + 待跟进）

已执行（不在并行会话在途文件内）：

- ✅ `now_ms()` 合并：core 侧 `ipc::server::now_ms`（pub）供 `market/host.rs` 复用；polymarket 侧 `feed::now_ms`（pub(crate)）供 `discovery.rs`/`live.rs` 复用——4 份副本 → 1 份实现（−21 行）。`service.rs:2878` 还有一份私有副本，在在途文件里，待跟进；
- ✅ panel refresher 双份闭包 → `fetch_snapshot_once`（见 §5）；
- ✅ `marketdata::LocalBook::update_top` 的两处「collect keys → 逐个 remove」→ 直接 `retain`（去 Vec 分配，−2 行 ×2）。

待跟进（锁定在并行会话在途文件）：

- `run_web_subcommand`：web.rs:19-117 vs blitzkrieg.rs:421-494 整段复制；
- CLI 解析 4 处重复（web.rs:35-58、app.rs:16-32、lib.rs:63-100、blitzkrieg.rs）；
- 快照装配 3 份（ipc_client.rs:264-310、gateway/command.rs:238-276、web/mod.rs:1279-1283）；
- service.rs book 落地/撮合两份（1319-1341+1372-1390 vs 2568-2595；ipc/server.rs:504-527 再一份）；
- 错误映射双份（ipc/server.rs:955-963 vs 972-982）；
- IPC 方法名常量与 ui_kit 裸字符串双轨。

## 7. 性能热点（已修 + 待跟进）

已修：

- ✅ `engine.rs:899-913` — fresh_book 双 snapshot → 先查 `timestamp`/`is_empty` 再分配（`evaluate` 路径不再为 stale/empty book 生成快照）；
- ✅ `marketdata.rs` — `snapshot()` 用 `from_sorted_levels` 砍 BTreeMap→Vec 的重复排序；`apply_snapshot` clear()+重建 → 差分 upsert（B-tree 节点复用，零节点 churn；带 zero-size 陈旧层回归测试）；`update_top` 两处 collect→retain；
- ✅ `engine.rs:326-351` — evaluate 全簿 snapshot 消除（fresh_book 早退后 confirmed 空路径不再构造快照）；
- ✅ `scanner.rs` — `RoundTiming`（无 markets 克隆的轮次时钟），`round_state`/`can_trade_reason`/evaluate 三处不再 clone 全量 markets；
- ✅ `strategy_logic::model` — `from_sorted_levels`（跳过防御性排序；`from_levels` 保留排序语义并委托）；
- ✅ `signal.rs` TrendTracker.on_price — entry owned-key → get_mut-first match（省每 tick String 分配）。

待跟进（在途文件）：

- `service.rs:2704` — 每 tick `self.books.clone()` 全量深拷贝；
- `service.rs:1319-1341`（dhat 证据：`engine_on_data@service.rs:1330/1331` 的 bids/asks Vec clone，2.56MB ×2 站点）；
- `ome.rs:107-118` — `live_orders()` 每调用全表收集+排序；
- tick 路径 `format!` 临时 String（service.rs:2652-2672/2747/2399）。

## 8. 优化结果（round1 + round2，全部复测）

### criterion（vs `e14-baseline`，同一 fixture）

| bench | 基线中位 | 复测中位 | Δ |
| --- | --- | --- | --- |
| book_apply/depth/5 | 5.588 µs | ~4.55 µs | −18.7% |
| book_apply/depth/20 | 8.883 µs | ~8.13 µs | −8.5% |
| evaluate/tokens/1 | 218.8 ns | ~19.7 ns | −91.0% |
| evaluate/tokens/4 | 788.4 ns | ~19.8 ns | −97.5% |
| engine_on_data/4tok | 9.877 µs | ~8.34 µs | −15.6% |
| tick/1pos | 1.142 µs | ~1.14 µs | 持平 |

P99 −20% 目标：evaluate 两组（−91%/−97.5%）远超；depth/5 −18.7% 接近；`engine_on_data`（−15.6%）与 `book_apply/depth/20`（−8.5%）未达标，剩余时间集中在 service.rs 的 bids/asks 镜像 clone（1330/1331 行）与每 tick `books.clone()`（2704 行）——均在在途文件里，落地后跟进（见 §7）。

> 中间过程记录：第一版差分实现用无条件 retain（O(existing × incoming) 恒扫），`book_apply/depth/20` 回归 +46%、`engine_on_data` +33%；加「`side.len() == valid` 时跳过 retain」后消除回归并转为改善。教训：O(n·m) 的集合 diff 必须先做 O(1) 相等性短路。

### 内存（dhat 口径，`tests/dhat_profile.rs`，同一 fixture）

| 指标 | 基线 | round2 | Δ |
| --- | --- | --- | --- |
| 总分配字节 | 29,980,123 B（28.59 MiB） | 17,436,084 B（16.63 MiB） | **−41.8%** |
| 总分配块数 | 76,337 | 44,337 | **−41.9%** |

主要来源：`apply_snapshot` 差分更新消除了每次全量快照的 B-tree 节点 churn（基线 top 站点：VacantEntry 5.89MB + InternalNode 3.72MB + 2×1.47MB），加上 `update_top` 去 collect。验收目标 −15% 达成（issue 点名 dhat/heaptrack 口径）。

RSS 口径（`e14-mem-round2.json`，30s 采样）：峰值 17,616 KB vs 基线 17,472 KB（+0.8%，曲线平稳无爬升）。macOS libmalloc 页级高水位效应：分配总量大减后，存留分配（async runtime、market 常驻结构）跨页锁住水位，RSS 不回落——与 round1 观察一致，RSS 不是本项的有效口径，以 dhat 为准。

### LOC（`e14-loc.json` 复测）

45,626 → **45,523**（−103 行，−0.23%）。目标 −10%（−4,563 行）的剩余差距锁定在：service.rs book 落地/撮合两份合并（1319-1390 vs 2568-2595，~300+ 行）、run_web_subcommand 整段复制（~80 行）、快照装配 3 份、CLI 解析 4 处——全部在并行会话在途文件（service.rs / ipc/* / ui_kit / web），E14 无法安全动，落地后跟进（见 §6 待跟进清单）。已执行项（now_ms 4 份、refresher 双份、update_top collect）按可达范围完成。

## 9. #149 收尾落地（perf/e14-remainder，2026-09-20）

§6/§7 待跟进项在独立 worktree（与并行会话在途文件隔离）落地后的复测（同 fixture、同机、main 9338b39 纯净对照）：

- **`service.rs` 每 tick `self.books.clone()` 全量深拷贝 → 共享不可变借用**：`run_exit_checks` 的 `book_fn` 闭包直接 `self.books.get(token)`，只克隆持仓实际触达的 token 层级，不再整表深拷贝；
- **`ome.rs` `live_orders()`/`live_for()` 先过滤后排序**：不再对全表排序后丢弃非 live 行；排序键与输出语义不变；
- **`service.rs` 私有 `now_ms()` 副本删除**：并入 E14 收敛后的 `ipc::server::now_ms`（6 处调用点复用）；
- **`run_web_subcommand` 整段复制合并**：`ui_kit/src/web/gateway_run.rs` 单一实现，`ui_kit_web` bin 与 `blitzkrieg web` 子命令都委托（顺带把 bin/web.rs 落后的信号收尾路径升到 spawn_blocking 版，`scripts/gateway-signal-stop-check.mjs` 门禁语义不变）；
- **ui_kit IPC 方法名裸字符串收编**为 `core/ipc_client.rs` 内 `method` 常量模块（ui_kit 按契约不链接 core crate，镜像为文本映射，面板每轮 poll 实证）。

复测数字（release、同 fixture、与纯净 9338b39 对照）：

- dhat（`tests/dhat_profile.rs`）：总分配 17,476,215 B → 16,319,443 B（**−6.62%**），总块数 48,343 → 45,543（−5.79%）；512,000 B/800 blk ×2 站点消失（按 200 tick × 4 × 640 B 计吻合「每 tick 全量 books 深拷贝」）。2.56 MB 家族（`engine_on_data` bids/asks 镜像 clone）按计划保留——消除它需要 `engine.on_data(&ev)` 借用签名变更，波及 engine.rs 全部调用点（并行会话在途），另行跟进；
- criterion（`--bench hot_path`，中位）：`tick/1pos` 1.147 µs → **841 ns（−26.7%）**，超过剩余 −20% 目标；`engine_on_data/4tok` −4.7%；`book_apply/depth/20` −2.9%；`evaluate/*` ±2–4%（ns 级噪声，未触碰该路径）。测试均在生产栈 + 并行会话活动负载下运行，绝对值偏保守；
- LOC（`e14-loc-count.mjs`）：49,396 → 49,390（−6 净）。经核实 §6 的「快照装配 3 份」「CLI 解析 4 处」「错误映射双份」在当前 main 已收敛（gateway/web 均委托 `IpcClient::snapshot`；CLI flag 集各不相同仅 `--socket` 四行模式重复；错误映射已是单 `map_err` 路径）——E14 记录的是当时在途态，落地前已不再是重复。剩余大头为 service.rs book 落地/撮合两份合并（~300+ 行），与并行会话 strategy 重构的 service.rs 在途改动邻接，待其落地后跟进。


## 约束记录

- 并行会话在途文件不触碰（mean_reversion / strategy_logic / service.rs / ipc / ui_kit web 系列）；
- benches/hot_path.rs 只读引用 `strategies/test_support.rs`（在途文件），bench 编译用 `--no-default-features` 时注意其 feature 依赖；
- `signal.rs` 的 get_mut-first 改动与并行会话在途状态叠加，属允许的优化项（历史决策）；
- 提交只含 E14 自有文件：docs/perf/*、scripts/e14-*、marketdata.rs、engine.rs、scanner.rs、strategy_logic/src/model.rs、signal.rs、polymarket discovery/live/feed、market/host.rs、ui_kit_panel bin/lib.rs、core/blitzkrieg_core/{Cargo.toml,tests/dhat_profile.rs}、benches/hot_path.rs、Cargo.lock。
