/// <reference types="vite/client" />

// Lets `import brandMark from '@/assets/logo.png'` type-check: Vite resolves the
// asset to a URL string at build time, and this reference supplies the ambient
// `*.png` module declaration that says so.
